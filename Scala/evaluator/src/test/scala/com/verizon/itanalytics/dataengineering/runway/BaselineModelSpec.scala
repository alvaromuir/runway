package com.verizon.itanalytics.dataengineering.runway

import org.scalatest.FlatSpec

class BaselineModelSpec extends FlatSpec with Builder with testutils.Utils with Evaluator {

  "the evaluator" should
  "read Baseline models" in {
    val status = "not yet implemented, requires a test file"
    assert(status != null)
  }

}
